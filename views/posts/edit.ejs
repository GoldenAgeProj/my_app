<!DOCTYPE html>
<html>
  <head>
    <% include ../partials/head %>
  </head>
  <body>
    <header>
      <% include ../partials/header %>
    </header>
    <div class="posts">
      <% include partials/top %>
      <!-- main start -->

      <!-- views/posts/edit.ejs -->
      <div class="edit">
        <h3>Edit Post</h3>
        <!-- 대부분의 브라우저들이 보안을 문제로 post를 제외한 나머지 신호들을 차단한다.
        따라서 이를 우회하기 위해 action에서 put 으로 override 한다.
        -->
        <form class="posts_form" action="/posts/<%=data._id%>?_method=put" method="post">
          <label>Title</label>
          <input type="text" name="post[title]" value="<%=data.title%>">
          <label>Body</label>
          <textarea name="post[body]"><%=data.body%></textarea>
          <button type="submit">Submit</button>
        </form>
        <a href="/posts/<%=data._id%>"><button>Back</button></a>
      </div>

      <!-- main end -->
    </div><!-- posts end -->
    <footer>
      <% include ../partials/footer %>
    </footer>
  </body>
</html>
